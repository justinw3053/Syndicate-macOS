# EXERCISE 13: Adding to Lists (Append)
#
# We can add an item directly to the end of any list using `.append()`:
#    my_list = [1, 2]
#    my_list.append(3)   # my_list is now [1, 2, 3]
#
# Your Task:
# Given a list called 'tokens', append the string "EOF" to it, and return the modified list.

def add_eof_token(tokens):
    # Step 1: Append "EOF" to 'tokens'
    tokens.append(...)
    
    # Step 2: Return 'tokens'
    return tokens
