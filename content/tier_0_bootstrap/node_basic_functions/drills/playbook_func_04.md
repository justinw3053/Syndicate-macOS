# Textbook: Re-using Functions with Arguments (KATA_FUNC_04)

## Theoretical Foundation
Functions act as isolated math blocks. We pass inputs (arguments) into functions to dynamically perform operations and return a final value.

---

## Step-by-Step Exercise Instructions
1. Multiply the parameter `x` by `y` using `result = x * y`.
2. Return `result`.
