# EXERCISE 5: Extracting Items (List Indexing)
#
# To read a single item out of a list, we use its position index in brackets.
# In programming, we always start counting positions at ZERO (0), not 1!
# Example:
#    colors = ["red", "green", "blue"]
#    first_color = colors[0]   # This gets "red"
#
# Your Task:
# Given a list called 'tokens', extract the very FIRST item (index 0) 
# and assign it to a new variable named 'first_token'.

def get_first_item(tokens):
    # Write your code on the line below! Type exactly: first_token = tokens[0]
    first_token = ...
    
    return first_token
