# EXERCISE 10: Filtering Items in Loops
#
# We often filter lists to keep only items matching a criteria.
# We create an empty list before the loop: `filtered = []`
# And add items that match using `.append()` inside the loop:
#    for num in numbers:
#        if num > 10:
#            filtered.append(num)
#
# Your Task:
# Complete the loop below to append only numbers that are strictly positive (> 0)
# to your 'filtered' list, then return it.

def filter_positives(numbers):
    filtered = []
    # Complete the loop, condition, and append steps below!
    for num in ...:
        if num > ...:
            filtered.append(...)
            
    return filtered
