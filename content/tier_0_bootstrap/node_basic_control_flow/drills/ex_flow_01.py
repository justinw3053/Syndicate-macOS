# EXERCISE 6: Making Decisions (if/else)
#
# Python uses "if" and "else" blocks to run code conditionally:
# Notice that we use exactly 4 spaces (Indentation) to show what belongs inside the blocks.
# Example:
#    if score > 50:
#        return "PASS"
#    else:
#        return "FAIL"
#
# Your Task:
# If 'score' is greater than or equal to 0.5, return "PASS".
# Otherwise, return "FAIL".

def check_passing_score(score):
    # Complete the conditional blocks below!
    if ...:
        return ...
    else:
        return ...
