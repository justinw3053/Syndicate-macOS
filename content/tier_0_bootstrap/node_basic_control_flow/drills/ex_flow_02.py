# EXERCISE 7: Checking Equality (==)
#
# In Python, a single equals sign (=) assigns a value to a variable.
# To check if two values are EQUAL, we use a double equals sign (==):
# Example:
#    if color == "red":
#        return "Stop!"
#
# Your Task:
# If 'status' is equal to the text "active", return True.
# Otherwise, return False.

def is_active(status):
    # Complete the conditional block below checking if status equals "active"
    if status == ...:
        return ...
    else:
        return ...
