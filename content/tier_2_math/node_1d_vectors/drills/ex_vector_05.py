# PROBLEM: Vector Normalization
# Normalizing a vector means scaling it so its magnitude (length) is exactly 1.0, 
# while preserving its direction. This is crucial for cosine similarity.
# Magnitude = sqrt(sum(v_i ^ 2))
#
# Your Task: Implement `normalize(vec: list[float]) -> list[float]`
# If the magnitude is 0, raise a ValueError("Cannot normalize zero vector").

def normalize(vec: list[float]) -> list[float]:
    # STUDENT_IMPLEMENTATION: Calculate magnitude, then divide each element by magnitude
    pass
