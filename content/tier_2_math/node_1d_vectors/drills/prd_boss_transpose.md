# PRD: Matrix Transposition
## Overview
To multiply matrices easily, we need to flip their axes: rows become columns, and columns become rows.
## Requirements
1. Implement a function `transpose(M: list[list[float]]) -> list[list[float]]`.
2. It must transpose an arbitrary M x N matrix into an N x M matrix.
3. You must use pure Python standard libraries only.
