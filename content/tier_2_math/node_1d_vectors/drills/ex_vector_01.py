# PROBLEM: Vector Addition & Scalar Multiplication
# Vector operations happen element-by-element. 
#
# Your Task: Implement add_vectors(vec_a: list[float], vec_b: list[float]) -> list[float]
# Use a standard `for` loop (e.g. iterating over range(len(vec_a))) to add corresponding elements.
# Append the sum to a new list and return it.
# DO NOT use list comprehensions. Master the loop first.

def add_vectors(vec_a: list[float], vec_b: list[float]) -> list[float]:
    # STUDENT_IMPLEMENTATION: Use a standard loop and .append()
    pass
