# BOSS FIGHT: Matrix Transposition
# Often, data comes in "rows" but we need it grouped by "columns" (e.g., flipping a matrix).
# Example: [[1, 2], [3, 4], [5, 6]] -> [[1, 3, 5], [2, 4, 6]]
#
# Your Task: Implement `transpose(matrix: list[list[float]]) -> list[list[float]]`
# Assume all inner lists have the exact same length. If matrix is empty, return [].

def transpose(matrix: list[list[float]]) -> list[list[float]]:
    # STUDENT_IMPLEMENTATION: Implement transposition
    pass
