# BOSS FIGHT: Greedy Sampling (Argmax)
# The simplest generation method is Greedy Sampling: just pick the token with the highest probability.
# 
# Your Task: Implement `argmax(probs: list[float]) -> int`
# Return the INDEX of the maximum value in the list.
# If the list is empty, raise ValueError.

def argmax(probs: list[float]) -> int:
    # STUDENT_IMPLEMENTATION: Find and return the index of the highest probability
    pass
