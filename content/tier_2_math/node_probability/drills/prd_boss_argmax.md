# PRD: Greedy Sampling (Argmax)
## Overview
In deterministic greedy text generation, the model simply selects the word with the highest overall probability.
## Requirements
1. Implement argmax(probs: list[float]) -> int
2. It must return the list index of the highest probability value, without importing external libraries.
