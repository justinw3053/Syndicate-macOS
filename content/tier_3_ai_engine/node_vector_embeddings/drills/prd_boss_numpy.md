# PRD: Vectorized Cosine Similarity (NumPy)
Write a function `vectorized_cosine_similarity(matrix: np.ndarray, query: np.ndarray) -> np.ndarray`.
It must calculate the cosine similarity between the query vector and every row in the matrix simultaneously using NumPy.
Do not use `for` loops.
