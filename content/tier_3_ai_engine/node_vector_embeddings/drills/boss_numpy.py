# BOSS FIGHT: The Big O Bottleneck (Vectorized Math)
# Pure python list operations are too slow for millions of vectors. 
#
# Your Task: Implement batch_cosine_similarity(query, matrix)
# Return a list of cosine similarities between the query and every vector in matrix.

def batch_cosine_similarity(query: list[float], matrix: list[list[float]]) -> list[float]:
    # STUDENT_IMPLEMENTATION: Calculate cosine similarities in a batch
    pass
