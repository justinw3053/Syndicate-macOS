# PROBLEM: Function Signature Extraction
# LLMs need to know what tools they can use. We provide this via a JSON Schema.
#
# Your Task: Implement extract_types(func_kwargs: dict) -> dict
# Given a dictionary of python type hints (e.g. {"name": str, "age": int}),
# return a dict mapping the argument name to its string type name ("string", "integer", "boolean").

def extract_types(func_kwargs: dict) -> dict:
    # STUDENT_IMPLEMENTATION: Map Python types to string representations
    pass
