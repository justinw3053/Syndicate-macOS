# PRD: Boolean Truthiness Safeguards

## Goal
Implement a truthiness classifier.

## Requirements
Return "missing" if data is None.
Return "zero" if data is the integer 0.
Return "empty_list" if data is an empty list [].
Return "valid" otherwise.
