# PRD: State Machine Router
Build a state transition validator.
Implement `is_transition_legal(routing_graph: dict, current: str, target: str) -> bool`.
Returns True if `target` exists in the list of valid destinations for `current` within `routing_graph`.
