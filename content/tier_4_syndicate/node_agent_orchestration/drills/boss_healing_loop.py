# BOSS FIGHT: Self-Correcting Execution Loops
# A key feature of agentic systems is the "healing loop" (e.g. Reflexion).
#
# Your Task: Implement execute_with_retries(func, max_retries: int) -> any
# Attempt to execute `func()`. If it raises an Exception, catch it and try again, up to max_retries times.
# If it fails on the final try, raise the Exception.

def execute_with_retries(func, max_retries: int):
    # STUDENT_IMPLEMENTATION: Implement retry loop
    pass
