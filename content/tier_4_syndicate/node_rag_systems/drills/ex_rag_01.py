# PROBLEM: Simple Chunking
# You cannot embed a whole book at once. You must chunk it.
#
# Your Task: Implement simple_chunk(text: str, chunk_size: int) -> list[str]
# Split the text into a list of strings, each exactly `chunk_size` characters long.
# The last chunk may be shorter.

def simple_chunk(text: str, chunk_size: int) -> list[str]:
    # STUDENT_IMPLEMENTATION: Implement character-level chunking
    pass
