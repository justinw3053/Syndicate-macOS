# BOSS FIGHT: Token-Overlapping Chunker
# To prevent cutting a sentence in half and losing context, RAG systems use overlapping chunks.
#
# Your Task: Implement overlap_chunk(words: list[str], chunk_size: int, overlap: int) -> list[list[str]]
# Return chunks of length `chunk_size`. The next chunk must step back by `overlap` words.

def overlap_chunk(words: list[str], chunk_size: int, overlap: int) -> list[list[str]]:
    # STUDENT_IMPLEMENTATION: Implement overlapping sliding window
    pass
