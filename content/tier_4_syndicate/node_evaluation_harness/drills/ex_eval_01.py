# PROBLEM: Socratic Error Messages
# Standard assertion errors are binary. We want to provide helpful, guiding feedback.
#
# Your Task: Implement evaluate_length(student_list: list, expected_len: int) -> tuple[bool, str]
# Return (True, "Success") if the length matches. 
# If it does not, return (False, "Your list has {len} elements, but it should have {expected_len}. Did you drop an item?")

def evaluate_length(student_list: list, expected_len: int) -> tuple[bool, str]:
    # STUDENT_IMPLEMENTATION: Implement Socratic evaluation
    pass
