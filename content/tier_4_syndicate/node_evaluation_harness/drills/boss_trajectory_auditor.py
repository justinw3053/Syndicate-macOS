# BOSS FIGHT: Trajectory Auditor
# Agent loops are validated by verifying their transition ledger.
# Write a trajectory checking algorithm.

def audit_trajectory(log: list[str], illegal_transitions: list[tuple[str, str]], mandatory_checkpoints: list[str]) -> list[str]:
    # STUDENT_IMPLEMENTATION: Check that no mandatory checkpoints were skipped, and no illegal transitions occurred.
    pass
