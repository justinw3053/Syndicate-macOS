# BOSS FIGHT: Strict JSON Schema Validator
# The evaluation harness must validate complex nested dictionaries exactly.
#
# Your Task: Implement strict_validate(payload: dict, schema: dict) -> list[str]
# Return a list of error strings (e.g. "Missing key: name", "Type mismatch on age: expected int, got str").
# If the payload is perfect, return an empty list [].

def strict_validate(payload: dict, schema: dict) -> list[str]:
    # STUDENT_IMPLEMENTATION: Implement deep dictionary validation
    pass
